# write another program 
# that prompts for a list of numbers as before
# until the user enters 'done'. 
# once 'done' is entered, print out the maximum and minimum of the numbers.
# if the user enters anything other than a number or done,
# detect their mistake using try and except 
# and print an error message and skip to the next number.

inp = 'Enter a number: '
minm = 0
maxm = 0



while True:
        s = raw_input(inp)
        if s == 'done':
                break
        try:
                minm = min(l)
                maxm = max(l)
                average = total / count
        except ValueError:
                print "Invalid Input. Try again: "
		continue
print 'You entered %s numbers whose total is %s and average is %s.' % (str(count), str(total), str(ave
